// Package transport provides the built-in artel.Transport implementations:
// HTTP for the real network and InProcess, a synchronous in-memory switchboard
// for deterministic tests.
package transport
